<h1>タスク新規作成ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :title, 'タスク名' %>
  <%= f.text_field :title %>
 
  <%= f.label :content, '詳細' %>
  <%= f.text_field :content %>

  <%= f.submit '登録' %>
<% end %>

<p><%= link_to '一覧に戻る', tasks_path %></p>