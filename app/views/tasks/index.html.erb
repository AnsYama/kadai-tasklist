<h1>タスク一覧</h1>
<ul>
    <% @tasks.each do |task| %>
        <li><%= link_to task.id, task %>【タイトル】<%= task.title %> &gt; 【 詳 細 】<%= task.content %> &gt; 【Status】<%= task.status %></li>
    <% end %>
</ul>
<%= link_to '新規タスクの登録', new_task_path %>